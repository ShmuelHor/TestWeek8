import React from 'react'
import { useSocket } from '../../hooks/useSocket';
const {} = useSocket();
const ThreatChart = () => {
  return (
    <div>
        
    </div>
  )
}

export default ThreatChart